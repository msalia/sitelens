# dependencies
**/node_modules/

# rust build artifacts
**/target/

# next.js
**/.next/
**/out/

# env files — .env is gitignored (it may hold real local secrets, e.g. the
# Cesium Ion token). Copy .env.example → .env to set up locally; production
# values live in the Dokploy env. Only .env.example is committed.
/.env
.env.local
.env*.local
**/.env.local
**/.env.production

# project
.dokploy.json

# test artifacts
**/test-results/
**/playwright-report/

# IDE / OS
.idea/
.vscode/
.DS_Store

# Local Docker overrides (ports, arch-specific images) — see .example
docker-compose.override.yml
